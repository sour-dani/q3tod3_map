name: Compile

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build-windows:
    runs-on: ubuntu-22.04
    
    steps:
      - uses: actions/checkout@v7

      - name: Compile
        shell: bash
        run: |
          g++ q3tod3_map.cpp -o q3tod3_map
      
      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: q3tod3_map-linux
          path: q3tod3_map
    
  build-linux:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v7

      - name: Compile
        shell: bash
        run: |
          g++ q3tod3_map.cpp -o q3tod3_map.exe
      
      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: q3tod3_map-windows
          path: q3tod3_map.exe